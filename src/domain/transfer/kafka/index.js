
const Publish = require('./publish')
const Consume = require('./consume')


const publish = (?) => {
    //TBD by laz
    return ??
}

const consume = (?) => {
    //TBD 
    return ??
}

module.exports = {
    publish,
    consume
}
